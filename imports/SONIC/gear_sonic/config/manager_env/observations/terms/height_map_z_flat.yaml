# Z-only height map for decoder prediction target.
# Returns (B, 121) — z-heights in robot body frame from 11x11 grid.
height_map_z_flat:
  _target_: isaaclab.managers.ObservationTermCfg
  func: gear_sonic.envs.manager_env.mdp.observations:height_map_z_flat
  params:
    command_name: "motion"
    random: False
