# Single-object smoke test: one apple.
#
# Usage:
#   conda run -n hunyuan python -m grail.pipelines.gen_3d_assets -i configs/gen_3d/apple.yaml -o data/gen_apple

- Red apple with a short brown stem and a single green leaf
